[workspace]
members = [
  "iroh",
  "iroh-bytes",
  "iroh-ffi",
  "iroh-metrics",
  "iroh-net",
]

[profile.optimized-release]
inherits = 'release'
lto = true
debug-assertions = false
opt-level = 3
panic = 'abort'
incremental = false

[profile.ffi]
inherits = 'release'
strip = "symbols"