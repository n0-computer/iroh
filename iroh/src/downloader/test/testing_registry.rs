//! An externally controlled [`AvailabilityRegistry`].
// NOTE: controlling the registry externally would be the idea in the future

// Testing registry we control
// NOTE: controlling the registry externally would be the idea in the future
// pub(super) struct TestingRegistry(Arc<RwLock<Registry>>);
